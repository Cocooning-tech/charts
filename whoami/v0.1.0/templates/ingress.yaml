apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: whoami
spec:
  rules:
  - host: verdun.cocooning.tech
    http:
      paths:
      - backend:
          serviceName: whoami
          servicePort: 80
        path: /whoami
        pathType: ImplementationSpecific
