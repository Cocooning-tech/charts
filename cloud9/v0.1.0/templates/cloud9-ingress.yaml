apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/rewrite-target: /ide.html
spec:
  rules:
  - host: "verdun.cocooning.tech"
    http:
      paths:
      - path:
        backend:
          serviceName: {{ .Chart.Name }}
          servicePort: {{ .Values.service.port }}
